/**
 * The package as one surface. Everything lives in ./gorsel — one file, because
 * the decisions in it are read together: the header says what the file is, the
 * format says what it can become, the size says whether it may start now.
 */

export * from "./gorsel.js";
